import { StyleSheet } from 'aphrodite/no-important';

const styles = StyleSheet.create({
  container: {
    width: '40px',
    height: '40px',
    position: 'relative',
    left: '87%',
    top: '-40px',
  },
});

export default styles;
